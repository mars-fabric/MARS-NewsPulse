# MARS-NewsPulse Backend Dependencies
# ------------------------------------
# The heavy ML + orchestration stack is pulled in by `cmbagent_infosys`.
# We pin only what NewsPulse itself imports directly (FastAPI, LangGraph
# adapters, PDF/chart renderers, HTTP clients).

# Core cmbagent framework (multi-agent orchestration + providers)
cmbagent_infosys==1.0.0

# Web stack
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
python-multipart>=0.0.9
websockets>=12.0
python-dotenv>=1.0.0

# Logging
structlog>=24.0.0

# HTTP clients (used by credential tests, news search, etc.)
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# LangGraph + LangChain adapters for the NewsPulse report pipeline
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# DuckDuckGo search for the discovery + analysis stages
ddgs>=6.0.0

# Markdown -> HTML -> PDF rendering for the final report
markdown>=3.6
weasyprint>=62.0
fpdf2>=2.7.0

# Sentiment dashboard charts
matplotlib>=3.8.0
numpy>=1.26.0

# PDF input extraction (used by file uploads / optional)
pymupdf>=1.24.0
pypdf>=4.0.0

# Credential encryption
cryptography>=42.0.0
