# MARS-NewsPulse Backend Dependencies
# ------------------------------------
# Version pins use compatible-release specifiers to balance reproducibility
# with security patches. Run `pip install -r requirements.txt` to install.

# Core framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
python-multipart>=0.0.9
websockets>=12.0
python-dotenv>=1.0.0

# Logging
structlog>=24.0.0

# HTTP clients
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.32.0

# LangGraph + LangChain adapters for the NewsPulse report pipeline
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-aws>=0.2.0

# CMB Agent (LLM provider registry + orchestration)
mars_cmbagent>=1.1.5

# DuckDuckGo search for the discovery + analysis stages
ddgs>=6.0.0

# Markdown -> HTML -> PDF rendering for the final report
markdown>=3.6
weasyprint>=62.0
fpdf2>=2.7.0

# Sentiment dashboard charts
matplotlib>=3.8.0
numpy>=1.26.0

# PDF input extraction (used by file uploads / optional)
pymupdf>=1.24.0
pypdf>=4.0.0

# Credential encryption
cryptography>=42.0.0

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
